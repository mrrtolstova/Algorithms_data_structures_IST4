
Задача: 

Реализовать игру Кен-кен.

Кен-кен – это математическая и логическая головоломка. Необходимо заполнить сетку цифрами так, чтобы в каждой строке и в каждом столбце они не повторялись.
Число в углу каждого выделенного блока является результатом арифметической операции над цифрами в этом блоке. 
В отличие от судоку-убийцы (сум-до-ку), цифры внутри блока могут повторяться.

Необходимо:

- реализвовать вывод интерфейса игрового поля 
- реализовать способ проверки правильного ответа
- найти способ задавать значения поля
- реализовать разделение поля на части в соответствии с правилами игры


Процесс:

(10.03.2020) + создан вывод игрового поля CreateMap()
(10.03.2020) + решено, что информация о поле будет браться из файла, реализовано открытие файла и запись матрицы из него 
(16.03.2020) + реализовано определение правильности ответа 
(16.03.2020) + реализовано разделение поля на области GenParts()
(16.03.2020) + реализовано назначение полям значений в соответствии с правилами	MakeRule()
(16.03.2020) + реализовано окрашивание областей поля в свои цвета из словаря colors

Реализованные функции:

• Init – Открывает и читает файл, вызывает функции CreateMap  и GenParts;
• CreateMap – Создает игровое поле (задает размер поля, добавляет клеточки, определяет расположение элементов);
• GenParts – Формирует области игрового поля;
• MakeRule – Создает правило заполнения для области;
• CompleteButton_Click – Проверяет верность ответа;
• AnswerButton_Click – Показывает ответ.

